# Project skills

Claude Code skills scoped to this repo go here (e.g. a `/new-service`
skill that scaffolds a backend service following the
routes → schemas → services → repositories layering, or a `/seed-demo-data`
skill for demo prep). None defined yet — add one when a workflow in this
repo becomes repetitive enough to be worth codifying.
